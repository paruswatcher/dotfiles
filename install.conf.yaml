- defaults:
    link:
        create: true
        relink: true

- clean: []
- link:
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.tmux.conf:
    ~/.vimrc:
    ~/.bashrc:

- shell: []
